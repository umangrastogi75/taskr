apiVersion: v1
kind: Service
metadata:
  name: taskr-service
spec:
  selector:
    app: taskr
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort   # use ClusterIP if behind an Ingress
